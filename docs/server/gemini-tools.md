# Gemini Tools

Location: `server/services/gemini/tools/`

Tools are registered with the Live manager to provide:

- Coaching hint triggers (`trigger_ui_coaching_hint`)
- File upload summarization

Tools return passive responses to prevent unintended model chatter.
